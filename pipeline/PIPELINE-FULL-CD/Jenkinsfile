pipeline {
    agent any
    stages {
        stage('Run Staging') {
            steps {
                // Get some code from a CodeCommit repository
                script{
                    def staging-job = build job: 'PIPELINE-FULL-STAGING', propagate: true, wait:true
                    echo staging-job.result
                }
            }
        }
    }
}
